@page
@model ListaZakupów.Pages.MojeListy.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>JednaLista</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="JednaLista.Id" />
            <div class="form-group">
                <label asp-for="JednaLista.Item" class="control-label"></label>
                <input asp-for="JednaLista.Item" class="form-control" />
                <span asp-validation-for="JednaLista.Item" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="JednaLista.IsBought" class="control-label"></label>
                <input asp-for="JednaLista.IsBought" class="form-control" />
                <span asp-validation-for="JednaLista.IsBought" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
